sudo: required
services:
    - docker

before_install:
    - docker build -t sonuajayin/react_frontend -f Dockerfile.dev ./prod_workflow/frontend/

script:
    - docker run sonuajayin/react_frontend npm run test #-- --coverage